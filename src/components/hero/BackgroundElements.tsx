import React from "react";

const BackgroundElements = () => {
  return (
    <div className="absolute inset-0 -z-10 overflow-hidden">
      {/* Background elements removed to improve Safari performance */}
    </div>
  );
};

export default BackgroundElements;